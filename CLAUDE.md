# NovaTerrum

Text-basiertes Spiel.

## Agent skills

### Issue tracker

Issues leben als GitHub Issues in `noledge5/NovaTerrum`. Siehe `docs/agents/issue-tracker.md`.

### Triage labels

Standard-Label-Vocabulary (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). Siehe `docs/agents/triage-labels.md`.

### Domain docs

Single-context: ein `CONTEXT.md` + `docs/adr/` im Root. Siehe `docs/agents/domain.md`.

<!-- TODO: Wenn das Projekt in klar getrennte Kontexte wächst (z.B. engine/, story/, ui/),
     CONTEXT-MAP.md im Root anlegen und auf Multi-context umstellen.
     Die Skills erkennen CONTEXT-MAP.md automatisch. -->
